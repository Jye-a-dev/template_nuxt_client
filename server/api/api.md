# api

EN: Stores server API route handlers. Files in this folder become backend endpoints under `/api` in the Nuxt/Nitro server.

VI: Chứa handler route API phía server. Các file trong thư mục này trở thành endpoint backend dưới `/api` trong Nuxt/Nitro server.
