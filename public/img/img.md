# img

EN: Stores public image files served directly from `/img/*`. Use this folder for static images that do not need build-time processing.

VI: Chứa hình ảnh public được phục vụ trực tiếp từ `/img/*`. Dùng cho ảnh tĩnh không cần xử lý trong quá trình build.
