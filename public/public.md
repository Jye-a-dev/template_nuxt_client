# public

EN: Stores static files served directly from the site root. Files here are not bundled by Vite and can be referenced by absolute URLs.

VI: Chứa file tĩnh được phục vụ trực tiếp từ root của website. File ở đây không được Vite bundle và có thể tham chiếu bằng URL tuyệt đối.
