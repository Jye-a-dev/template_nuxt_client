# plugins

EN: Stores Nuxt plugins that run during app initialization. Use this folder to inject clients, register libraries, or configure app-wide behavior.

VI: Chứa plugin Nuxt chạy khi ứng dụng khởi tạo. Dùng để inject client, đăng ký thư viện hoặc cấu hình hành vi toàn ứng dụng.
