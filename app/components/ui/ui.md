# ui

EN: Stores small shared UI components such as buttons, inputs, cards, badges, and reusable interface primitives.

VI: Chứa các UI component nhỏ dùng chung như button, input, card, badge và các thành phần giao diện nền tảng có thể tái sử dụng.
