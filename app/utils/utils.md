# utils

EN: Stores small pure helper functions that do not depend on Vue state or Nuxt runtime behavior.

VI: Chứa các helper function nhỏ, thuần logic, không phụ thuộc vào state của Vue hoặc runtime của Nuxt.
